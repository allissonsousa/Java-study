/// modulo chamador passa uma estrutura de daos para um modulo chaado
/// q utiliza um pequeco subconjunto de dados dessa estrutura

public class Pedido{
    private Date data;
    private int local;
    private List<ItemPedido> itens;
    private CLiente cliente;
    private Endereco enderecoEntrega;
    private Pagamentos;

    //restante da classe pedido
}

public class CalculadoraFrete {
    public BigDecimal obterValorFrete(Pedido pedido) {
        // algoritmo de calculo do valor de frete é feito utilizando apenas
        // o endereço de destino do pedido
    }
}

public class Exemplo {
    CalculadoraFrete calculadoraFrete;

    public void fecharPedido(Pedido pedido) {
        calculadoraFrete.obterValorFrete(pedido)
    }
}