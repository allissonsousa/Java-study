//pacote composto por varias classes
package primeiroprograma; 
        //classe
public class PrimeiroPrograma { //classes são composta por metodos
                  //metodo main ou prncipal
    public static void main(String[] args) { 
        System.out.println("Olá mundo!");
    }
}

//existe diferença entre letras maiusculas e minusculas CASESENSITIVE

//TECNICAS DE DIFERENCIAÇÃO DE IDENTIFICADORES
///Camel case >>
///MinhaNovaClasse se a primeira letra é maiuscula, indica que o é o nome de uma classe ou interface
///meuNovoAtributo se a primeira letra é minuscula e o resto segue a regra do Camel Case > pode ser um atributo, variavel ou metodo ou parametro 
///só letra minuscula é indica o nome do pacote > meupacote
///só letra maiuscula indica uma constante > VALOR_DE_PI > nao muda é constante
