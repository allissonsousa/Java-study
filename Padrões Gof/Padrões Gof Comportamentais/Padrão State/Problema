///o objeto vai ter um estado para cada situacao
///usado em sensores e recursos fisicos
/// cada estado é representado em uma classe
///pode ser usado com um pool de objetos flyweight ou hashmap para evitar a criaçao desnecessaria e repetitiva de objetos


public class PDV {
    private int estado;

    public iniciarSangria() {
        if (estado == ESTADO_PDV.Disponivel) {
            //tratamento da sangria quando o pdv esta disponivel
            //estado = sangria normal
        } else if (estado == ESTADO_PDV.Vendendo) {
            //tratamento da sangria quando PDV está vendendo
            //estado =  sangria expressa
        } else {
            //evento nao deve ser aceito
        }
    }
}